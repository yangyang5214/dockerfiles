version: "3.5"

services:
  weditor:
    image: beer5215/golang-crossbuild-debian10
    build:
      context: .
      dockerfile: Dockerfile
    container_name: golang-crossbuild-debian10
    restart: always

